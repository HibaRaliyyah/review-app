
  const name = "Hiba";
  const age = 19;

  const users = [

    { id: 1, text: "User 1", age: 30 },
    { id: 2, text: "User 2", age: 20 },
    { id: 3, text: "User 3", age: 38 }
  ];


  const loading = false;

  if (loading) return <h1>Loading.......</h1>

  const showUsers = true;

  const userBlock = (<div className="user-block">
    <ul>
      {users.map((user, index) => (
        <li key={user.id}>{user.text}</li>
      ))}
    </ul>
  </div>)

  return (

    <div className="container">
      <h1>Name: {name.toUpperCase()}</h1>
      <p>Age: {age}</p>

      <span>{5 + 5}</span>

      <h3>User Length: ({users.length})</h3>

      <h2>{showUsers ? "YES" : "NO"}</h2>

      {showUsers && (userBlock)
      }
    </div>


  );